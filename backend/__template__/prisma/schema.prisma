generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Example {
  id        Int      @id @default(autoincrement())
  name      String
  created_at DateTime @default(now())

  @@map("examples")
}
